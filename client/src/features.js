// Switches for parts of the app that are built but not shown yet.
export const features = {
  // The receipt PDF (the "View receipt" button on the payment confirmation and the "Receipt" link on each row).
  // Set to true to bring it back. The receipt number itself is always shown, and the server keeps the PDF ready.
  receipts: false,
};
